# App-specific overrides for @lumiris/site.
# Common vars live in ../.env.example.shared — run `make env-init` from the
# repo root to produce a full apps/site/.env (shared + overrides).

NEXT_PUBLIC_APP_NAME=site
PORT=3000
