{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "default",
    "description": "Default capability for Lumiris Vision — exposes compute_score and core APIs (desktop preview + Android).",
    "windows": ["main"],
    "permissions": ["core:default"]
}
