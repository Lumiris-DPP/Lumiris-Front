# App-specific overrides for @lumiris/admin.
# Common vars live in ../.env.example.shared — run `make env-init` from the
# repo root to produce a full apps/admin/.env (shared + overrides).

NEXT_PUBLIC_APP_NAME=admin
PORT=3001
