import { ValidationQueue } from '@/features/validation';

export const metadata = { title: 'Validation · LUMIRIS Admin' };

export default function ValidationPage() {
    return <ValidationQueue />;
}
