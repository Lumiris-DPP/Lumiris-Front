# App-specific overrides for @lumiris/client.
# Common vars live in ../.env.example.shared — run `make env-init` from the
# repo root to produce a full apps/client/.env (shared + overrides).

NEXT_PUBLIC_APP_NAME=client
PORT=3003
